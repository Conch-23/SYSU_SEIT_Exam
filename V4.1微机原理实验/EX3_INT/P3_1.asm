;p3_1
		ORG	0000H
		LJMP	START
		ORG	0003H
		LJMP	IN0
		ORG	0013H
		LJMP	IN1
		ORG	0030H
START:	MOV	SP, #60H
		MOV	TCON, #01H
		SETB	EA
		SETB	EX0
		SETB	EX1
		SETB	PX1
		CLR		PX0
		MOV 	DPTR, #TABLE
		MOV	PSW, #00H
		MOV	R2, #6
DISP: 	ACALL	DISPLAY
		SJMP 	DISP

DISPLAY: MOV	R1, #100
DISP0:	MOV	R0, #6
		MOV	R3, #0E7H  
DISP1:	INC		R3
		MOV	A, R2
		MOVC 	A, @A+DPTR
		MOV 	P0, A
		MOV 	P1, R3
		LCALL	DELAY         
		DJNZ	R0, DISP1
		DJNZ	R1, DISP0
		RET
TABLE:	DB 0C0H, 0F9H, 0A4H, 0B0H, 99H, 92H, 82H, 0F8H
		DB 80H, 90H, 88H, 83H, 0C6H, 0A1H, 86H, 8EH
IN0:		PUSH	ACC
		PUSH	PSW
		MOV	PSW, #10H
		MOV	R2, #0
		ACALL	DISPLAY
		POP		PSW
		POP		ACC
		RETI
IN1:		PUSH	ACC
		PUSH	PSW
		MOV	PSW, #18H
		MOV	R2, #1
		ACALL	DISPLAY
		POP		PSW
		POP		ACC
		RETI
DELAY:	MOV 	R6, #4  
DEL1:	MOV 	R7, #229
DEL2:	DJNZ 	R7, DEL2
		DJNZ 	R6, DEL1
		RET
		END